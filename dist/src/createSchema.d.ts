import { JsonSchema } from "./types";
/**
 * Create a simple json schema for the given input data
 * @param  data - data to get json schema for
 */
export declare function createSchema(data: unknown): JsonSchema | undefined;
