import { JsonSchema } from "../../types.js";
import { SchemaNode } from "../../types.js";
export declare function addRemotes(node: SchemaNode, schemaList: JsonSchema[], $schema: string, baseURI?: string): void;
